# Copy this file to .env (same folder as docker-compose.yml) and fill in real values.
# Used by docker-compose.yml for variable substitution.

# Privy dashboard → App settings → copy App ID and App Secret
NEXT_PUBLIC_PRIVY_APP_ID=changeme
PRIVY_APP_SECRET=changeme
